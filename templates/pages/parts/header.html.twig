<header>
    <div id="header">
        <div class="row">
            <div class="col-md-4">
                {% if app.user %}
                    Zalogowany jako {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Wyloguj</a>
                {% endif %}
            </div>
            <div class="col-md-4 text-center">
                <h2 class="main-title">Fiszki</h2>
            </div>
        </div>
    </div>
</header>